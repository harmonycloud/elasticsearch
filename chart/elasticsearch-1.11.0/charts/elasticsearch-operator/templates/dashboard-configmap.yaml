kind: ConfigMap
metadata:
  name: {{ include "elasticsearch-operator.fullname" . }}-dashboard
  labels:
    grafana_dashboard: "1" 
apiVersion: v1
data:
{{ (.Files.Glob "dashboards/*").AsConfig | indent 2 }}